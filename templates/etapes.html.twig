{% extends 'base.html.twig' %}

{% block body %}
	<h1 class="text-center">Liste des étapes</h1>
	<table class="table">
		<thead>
			<tr>
                <th scope="col">cours</th>
				<th scope="col">titre</th>
				<th scope="col">description</th>
				<th scope="col">contenu</th>
			</tr>
		</thead>
		<tbody>

			{% for etape in etapes %}
				<tr>

					<td>{{etape.cours.titre}}</td>
					<td>{{etape.titre}}</td>
					<td>{{etape.description}}</td>
					<td>{{etape.contenu}}</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>

{% endblock %}
